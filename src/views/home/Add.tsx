import {Text, View} from 'react-native';
import common from '../../styles/sharedStyles';

const Add = () => {
  const {flex1, aiCenter, jcCenter} = common;
  return (
    <View style={[flex1, aiCenter, jcCenter]}>
      <Text>Add Under construction!</Text>
    </View>
  );
};

export default Add;
